---
import HeroImage from '../components/HeroImage.astro';
import Base from '../layouts/Base.astro';
import Figure from '../components/Figure.astro';
import { businessName, phone, phoneHref, services, identity, positioning, fact } from '../data/brief';
import { SHOTS, shotsBy, heroSrc, haveCount, floorCount } from '../data/shotlist';
import { MARKETS } from '../data/markets';

/* Structure follows the reference the client chose (a 13-section turf landing page):
   hero → true numbers → gallery → story → standard → services → differentiators →
   process → before/after → FAQ → form → close. Two of its sections are NOT reproduced:
   a review count-up and a "10,000+ homeowners" claim. We have neither sourced, and
   inventing them is precisely what the old site was caught doing. The count-up mechanic
   runs on true numbers instead; the AI-visualizer slot becomes a scroll-scrubbed
   before/after on our own shot list. */

const founded  = fact<number>(identity?.founded_year);
const materials = fact<string>(positioning?.materials);
const hero = heroSrc();
const DEV = import.meta.env.DEV;

const ANSWER = {
  question: 'Who installs artificial turf and putting greens in Denver, Myrtle Beach and Jacksonville?',
  answer: `${businessName} installs artificial turf and backyard putting greens across the Denver metro, the Grand Strand in South and North Carolina, and northeast Florida. Residential lawns, pet turf, commercial installs, indoor facilities and sports fields. The founder has thirteen years in turf. Call ${phone} for a quote.`,
};

const SVC = [
  { slug: 'putting-greens',       name: 'Putting greens',        line: 'A green that rolls wrong gets used twice, then ignored. Roll lives underneath — base, fringe, cup, break, all designed before anything is cut.' },
  { slug: 'residential-turf',     name: 'Residential lawns',     line: 'Your soil moves and turf will not stop it. Front Range clay swells up to twenty percent by volume when wet; the coast is sand over a shallow water table. The base is what changes.' },
  { slug: 'pet-turf',             name: 'Pet turf',              line: 'If the yard smells in August, that is the base, not the grass. Florida wrote the drainage half into a rule for single-family lots — permeability through the turf, the backing and the subgrade, and no compacting the soil beneath it.' },
  { slug: 'commercial-turf',      name: 'Commercial',            line: 'You have been told Colorado banned turf on commercial property. Half right: from 1 January 2026 the prohibition covers nonfunctional turf on commercial, institutional and HOA-common land, and functional turf is defined at C.R.S. 37-99-102(6.5) and stays legal.' },
  { slug: 'sports-field-turf',    name: 'Sports fields',         line: 'Someone on the board will ask how hot it gets, and they should. Measured over a full day synthetic averaged 117°F against 78°F for grass, which makes shade and siting the argument rather than product.' },
  { slug: 'indoor-turf',          name: 'Indoor facilities',     line: 'Indoors nothing rinses it and nothing dries it. In a 2020 laboratory study MRSA stayed viable longest in sand infill — 27 hours to half its viability, against 2 hours on EPDM rubber — so a cleaning schedule belongs in the job, not after it.' },
];

const DIFF = [
  { h: 'American-made turf',            p: 'The owner’s words for what goes in the ground: all American-made stuff, premium stuff. We print his sentence, not ours.' },
  { h: 'Your soil sets the base depth', p: 'A backing can drain ten inches an hour. Your ground may not — that is New Jersey DEP’s line, not ours. Front Range clay swells. Coastal water tables sit inches down. The base changes, and the quote says how.' },
  { h: 'Thirteen years, and they are his', p: 'Timeless started in 2024. Thirteen years of installing turf came before it. Two numbers, and you get both, because the company’s age and the installer’s are not the same thing.' },
  { h: 'Count the leaves',              p: 'Pull up any installer’s site. Find their photo of their own putting green. Count the leaves on it. In his words: you can’t even blow off your green. That is his test of other companies — run it on ours too.' },
];

const pair = (id: string) => ({ b: SHOTS.find((s) => s.pairId === id && s.kind === 'before')!, a: SHOTS.find((s) => s.pairId === id && s.kind === 'after')! });
/* A wipe scrubs an AFTER photograph across a BEFORE photograph. With neither on file the
   section shipped four empty frames wearing "Before" and "After" tags, under a heading
   inviting you to watch it change. Only pairs where BOTH photographs exist are rendered,
   and the section disappears entirely until at least one does. */
const PAIRS = ['ba-1', 'ba-2', 'ba-3', 'ba-4'].filter((id) => { const p = pair(id); return !!p.b?.file && !!p.a?.file; });
---
<Base
  title="TIMELESS Grass & Greens — Artificial Turf & Putting Greens"
  description={`Artificial turf, pet turf and backyard putting greens across Denver, the Grand Strand and northeast Florida. Founder with 13 years in turf. Call ${phone}.`}
  answer={ANSWER}
>

  <!-- 1 · HERO -->
  <section class="hero hero--home">
    <HeroImage file={hero} />
    <div class="wrap">
      <p class="hero__eyebrow">Turf, pet turf &amp; putting greens</p>
      <h1>
        <span class="line"><span>You&rsquo;ve seen bad turf.</span></span>
        <span class="line"><span>The base <em>decides.</em></span></span>
      </h1>
      <p class="hero__deck">The turf is the easy part. Ripples, puddles and smell all start underneath — and Denver clay heaves, Grand Strand sand drains, and in northeast Florida the problem is heat and where the shade falls.</p>
      <p class="hero__qual">
        {materials && <><b>American-made turf</b> · </>}<b>Founder with 13 years</b> · <b>Free estimate, measured on site</b>
      </p>
      <div class="hero__cta">
        {phoneHref && <a class="btn btn--primary" href={phoneHref} data-cta="call-hero">Call {phone}</a>}
      </div>
    </div>
    <div class="hero__steps wrap">
      <div><i>1</i><span>You call, or send photos of the space</span></div>
      <div><i>2</i><span>We measure the space, the grade and the drainage</span></div>
      <div><i>3</i><span>You get a written number before work starts</span></div>
    </div>
  </section>


  <!-- 2 · TRUE NUMBERS — the count-up mechanic on facts the client stated -->
  <section class="sec">
    <div class="wrap">
      <div class="kick" data-reveal>
        <p class="micro">Prove it before I read</p>
        <h2>The numbers we can actually back.</h2>
      </div>
      <div class="nums">
        <div class="num" data-reveal><b data-count="13">13</b><span>years Brian has been installing turf. His years, not the company&rsquo;s.</span></div>
        {founded && <div class="num" data-reveal><b data-count={founded}>{founded}</b><span>the year Timeless opened. Rounding it up would have been easy. We didn&rsquo;t.</span></div>}
        <div class="num" data-reveal><b data-count="3">3</b><span>markets you can hire us in: Denver metro, Grand Strand, northeast Florida</span></div>
        <div class="num" data-reveal><b data-count="4">4</b><span>states whose turf laws we&rsquo;ve written up and cited. Read them before you sign.</span></div>
      </div>
      <!-- HOLE: no review count, no customer count, no rating. None is sourced.
           The reference shows "10,000+ homeowners" and a review count-up; we do not. -->
    </div>
  </section>


  <!-- 3 · UNDER THE TURF — the H1 says the base decides. This is where it is proved.
       Every sentence is markets.ts's own `ground` field, sourced per market. -->
  <section class="sec sec--green on-dark turf-marks turf-marks--dark">
    <div class="wrap">
      <div class="kick" data-reveal>
        <p class="micro">What you are actually paying for</p>
        <h2>Same turf. Three different bases.</h2>
        <p>The blades are the part you can see. Whether the surface ripples, ponds or smells in three years is decided by the ground it was built on — and that ground is not the same in the three places we work.</p>
      </div>
      <ul class="cards">
        {MARKETS.map((m) => (
          <li class="card" data-reveal>
            <h3>{m.short}</h3>
            <p>{m.ground}</p>
            <a href={`/${m.slug}/`} data-cta={`market-${m.slug}-ground`}>What the ground does here</a>
          </li>
        ))}
      </ul>
    </div>
  </section>


  <!-- 4 · GALLERY — drifts horizontally with scroll -->
  <section class="strip sec--dark on-dark">
    <div class="wrap"><div class="kick" data-reveal>
      <p class="micro">The work</p>
      <h2>Real yards. Ours.</h2>
      {DEV && haveCount() < floorCount && <p>Build note: {haveCount()} of {floorCount} client photographs on file. Every image below is a labeled stock stand-in for layout review, and none of them ships.</p>}
    </div></div>
    <div class="strip__track">
      {[...shotsBy('finished').slice(1), ...shotsBy('context'), ...shotsBy('detail')].map((shot) => <Figure shot={shot} sizes="clamp(280px, 38vw, 520px)" />)}
    </div>
  </section>


  <!-- 5 · STORY -->
  <section class="sec sec--band">
    <div class="wrap story">
      <div data-reveal>
        <p class="micro">Who shows up</p>
        <h2>You deal with the owners. First call to final walkthrough.</h2>
        <p>Timeless opened in 2024. Brian has been installing turf for thirteen years. Both are true, and rounding one into the other would have been easy — it is the first thing we decided not to do. What you are hiring is the thirteen years.</p>
        <p>The number comes from someone standing in your yard, not from a square-footage calculator. Base depth changes with the soil — Denver clay is not Grand Strand sand — so the measuring happens before the price does.</p>
        {phoneHref && <a class="btn btn--primary" href={phoneHref} data-cta="call-story">Call {phone}</a>}
      </div>
      <div class="story__fig" data-reveal>{shotsBy('finished').slice(4, 5).map((shot) => <Figure shot={shot} />)}</div>
    </div>
  </section>
  <!-- 6 · BEFORE / AFTER — scroll wipes the after over the before. Gated on real
       pairs; it renders nothing until Brian sends them. Placed in the proof cluster, not
       after the closing CTA where it used to sit. -->
  {PAIRS.length > 0 && (
  <section class="sec sec--green on-dark turf-marks turf-marks--dark">
    <div class="wrap">
      <div class="kick" data-reveal>
        <p class="micro">Same spot, twice</p>
        <h2>Scroll, and watch it change.</h2>
        <p>The same angle before we started and after we left. Keep scrolling — the after wipes across.</p>
      </div>
      <div class="wipes">
        {PAIRS.map((id) => { const p = pair(id); return (
          <div>
            <div class="wipe" data-reveal>
              <span class="wipe__tag wipe__tag--b">Before</span><span class="wipe__tag wipe__tag--a">After</span>
              <Figure shot={p.b} />
              <div class="wipe__after"><Figure shot={p.a} /></div>
              <span class="wipe__line"></span>
            </div>
            {p.a.caption && <p class="wipe__cap">{p.a.caption}</p>}
          </div>
        ); })}
      </div>
    </div>
  </section>
  )}
  <!-- 7 · SERVICES -->
  <section class="sec">
    <div class="wrap">
      <div class="kick" data-reveal>
        <p class="micro">What we install</p>
        <h2>Anything that&rsquo;s turf.</h2>
      </div>
      <div class="cards">
        {SVC.map((s, i) => (
          <div class="card" data-reveal>
            <span class="card__n">0{i + 1}</span>
            <h3>{s.name}</h3>
            <p>{s.line}</p>
            <a href={`/services/${s.slug}/`} data-cta={`svc-${s.slug}`}>How we do it &rarr;</a>
          </div>
        ))}
      </div>
    </div>
  </section>


  <!-- 8 · PROCESS — pinned, scroll advances the steps -->
  <section class="proc sec sec--green on-dark turf-marks turf-marks--dark">
    <div class="wrap proc__in">
      <div>
        <p class="micro">How a job goes</p>
        <h2 style="color:var(--ink-on-dark)">You call. We measure.<br />You get a number.</h2>
        <div class="proc__bar"><div class="proc__fill"></div></div>
      </div>
      <div class="proc__steps">
        <div class="proc__step"><i>01</i><h3>You call, or send photos</h3><p>The first question is what the space is for. Dogs, kids, putting, a commercial site — that changes the turf and the infill before it changes the price.</p></div>
        <div class="proc__step"><i>02</i><h3>Somebody comes out and measures</h3><p>Base depth comes off your soil, not a chart, and grade and drainage get checked before any number exists. A backing that can drain ten inches an hour is worth nothing if the ground under it will not take the water.</p></div>
        <div class="proc__step"><i>03</i><h3>You get a number in writing</h3><p>Material options, layout and next steps, in writing before any work begins. If a cheaper turf will do the job you are told which one and what you give up — you get what you pay for, and nobody here is going to pretend otherwise.</p></div>
        <div class="proc__step"><i>04</i><h3>You approve the design, then we build</h3><p>Nothing is cut until you have approved the design. Then base, turf, infill, edges — site prep and cleanup are ours from the first day to the last.</p></div>
      </div>
    </div>
  </section>
  <!-- 9 · DIFFERENTIATORS — six things, all stated by the client -->
  <section class="sec sec--dark on-dark">
    <div class="wrap">
      <div class="split">
        <div class="split__head" data-reveal>
          <p class="micro">Why us, in his words</p>
          <h2>Not every turf company is the same.</h2>
          <p>Four things that are true. Each one is checkable — ask on the call.</p>
          {phoneHref && <a class="btn btn--light" href={phoneHref} data-cta="call-diff">Call {phone}</a>}
        </div>
        <div class="diffs">
          {DIFF.map((d) => <div class="diff" data-reveal><h3>{d.h}</h3><p>{d.p}</p></div>)}
        </div>
      </div>
    </div>
  </section>


  <!-- 10 · THE STANDARD — dark band, stripes drift -->
  <section class="sec sec--green on-dark turf-marks turf-marks--dark turf-marks--drift">
    <div class="wrap band">
      <div class="band__copy" data-reveal>
        <p class="micro">How he grades a competitor</p>
        <h2>Zoom in on their green. Count the leaves.</h2>
        <p class="band__p">Not the design. Not the wording. The putting surface in the one photograph they chose to lead with. A blower costs nothing and takes two minutes — so if a crew skipped it on the shot everybody sees first, ask what else got skipped the day they drove away. That is his whole test, and it is cheap to meet and impossible to fake.</p>
        {phoneHref && <a class="btn btn--light" href={phoneHref} data-cta="call-standard">Call {phone}</a>}
      </div>
      <div class="band__fig" data-reveal>{shotsBy('finished').slice(5, 6).map((shot) => <Figure shot={shot} />)}</div>
    </div>
  </section>


  <!-- 11 · FAQ -->
  <section class="sec">
    <div class="wrap">
      <div class="kick" data-reveal>
        <p class="micro">Before you call</p>
        <h2>The questions we actually get.</h2>
      </div>
      <div class="faq" data-reveal>
        <details class="qa"><summary><h3>Does artificial turf get hot?</h3></summary>
          <p>Yes. Hotter than asphalt. Measured across a full day, synthetic surfaces averaged about 117&deg;F against 78&deg;F for natural grass and 110&deg;F for asphalt, with a peak of 200&deg;F on a 98&deg;F day (Williams and Pulley, Brigham Young University). If an installer tells you it runs cooler than your driveway, they are wrong.</p>
          <p>It is a contact problem, not an air problem. A 2024 review of 23 studies found air temperature over turf only 1&ndash;2&deg;F higher. Fine in shoes. Real for bare feet and paws. Shade does more than any infill swap — in that same review shade was worth about 25&deg;C, against 3.2 to 7.9&deg;C for changing infill. Where a spot is wrong for turf, we say so.</p>
        </details>
        <details class="qa"><summary><h3>Are there rebates for tearing out my lawn?</h3></summary>
          <p>Not in Horry County. We searched the Grand Strand Water &amp; Sewer Authority programs and the Horry County, Myrtle Beach and North Myrtle Beach codes. No turf rebate, no water rebate, no landscape-conversion incentive. Colorado and Florida differ by water district, and most Western rebates pay for living plantings rather than synthetic turf — Castle Rock Water pays $3.25 a square foot and excludes artificial turf in writing.</p>
          <p>Test any rebate claim in ninety seconds. Ask for the program name, the URL, the dollar amount per square foot, and the sentence in the rules that says synthetic turf qualifies. A real program has all four. Ask us before you count on any of it applying to your address.</p>
          <p class="qa__links"><a href="/guides/no-turf-rebates-grand-strand/" data-cta="guide-rebates-sc">Why there are no turf rebates in Horry County</a><a href="/guides/colorado-water-rebates-and-turf/" data-cta="guide-rebates-co">Denver-metro water rebates and artificial turf</a></p>
        </details>
        <details class="qa"><summary><h3>What does it cost?</h3></summary>
          <p>Per job, after measuring. The base is most of the work, so the number comes from what is under your yard rather than what goes on top of it. Square footage, access, what comes out, drainage and the product all move it. A small job costs more per square foot than a large one, because a day on site costs the same either way.</p>
          <p>If two quotes are thousands apart, find out what changed. Ask both for base depth, roll widths and seam count, and infill weight per square foot — the parts you cannot see once the turf is down. Cheap turf and premium turf are both real products. In the owner&rsquo;s words, you get what you pay for, and the quote names which one you are getting.</p>
        </details>
        <details class="qa"><summary><h3>Will it smell if I have dogs?</h3></summary>
          <p>Only if drainage fails. The blades are inert — urine passes straight through them and settles in the infill and at the backing, which is why washing the grass never fixes it. The smell is not in the grass.</p>
          <p>A pet run needs infill and a base built to move liquid, and that is decided before anyone unrolls turf. Florida wrote the infill half of it into a rule.</p>
          <p class="qa__links"><a href="/guides/florida-turf-infill-rules/" data-cta="guide-infill">What northeast Florida allows as infill</a></p>
        </details>
      </div>
    </div>
  </section>


  <!-- 12 · SERVICE AREAS — "do you come to my town" had been a footnote inside the form
       band at 87% depth. Towns are markets.ts `towns` only: the places the client named on
       the call or on his own site. src/data/cities/ holds 170 researched towns; those are
       page candidates, not a coverage claim, and are deliberately not listed here. -->
  <section class="sec sec--band">
    <div class="wrap">
      <div class="kick" data-reveal>
        <p class="micro">Do you come to my town</p>
        <h2>Three markets, and the towns he named in each.</h2>
      </div>
      <ul class="mkts" id="markets">
        {MARKETS.map((m) => <li><a href={`/${m.slug}/`} data-cta={`market-${m.slug}`}><span class="mkts__n">{m.name}</span><span class="mkts__s">{m.towns.join(' · ')}</span></a></li>)}
      </ul>
      <p class="proof__note">Not on the list? Each market runs wider than the towns named here — the market page carries the whole area, and a call settles it in a minute.</p>
    </div>
  </section>


  <!-- 13 · ANSWER BOX — authored for extraction -->
  <section class="sec answer-box"><div class="wrap" data-reveal>
    <p class="micro">The short version</p>
    <h2 class="answer-box__q">{ANSWER.question}</h2>
    <p class="answer-box__a">{ANSWER.answer}</p>
  </div></section>


  <!-- 14 · FORM -->
  <section class="sec sec--band" id="quote">
    <div class="wrap formband">
      <div data-reveal>
        <p class="micro">Get a number on it</p>
        <h2>{phone} is the owner&rsquo;s phone.</h2>
        <p>Not an office line. Tell him what the space is for — dogs, kids, putting, or a commercial site — because that one answer changes the product and the base. If it is easier to type it, the form is four fields.</p>
      </div>
      <!-- THE one form. Lead destination is not wired: brief.conversion.lead_destination is UNKNOWN. Gate 10 blocks launch until all eight test_lead flags are true. -->
      <form class="quote" name="quote" method="POST" action="/thanks/" data-netlify="true" netlify-honeypot="company" data-cta="quote-form" aria-labelledby="quote-h" data-reveal>
        <input type="hidden" name="form-name" value="quote" />
        <p class="hp" aria-hidden="true"><label>Company <input type="text" name="company" tabindex="-1" autocomplete="off" /></label></p>
        <p class="micro micro--accent" id="quote-h">Request a free estimate</p>
        <div class="quote__row">
          <label class="fld"><span>Name</span><input type="text" name="name" autocomplete="name" required /></label>
          <label class="fld"><span>Phone</span><input type="tel" name="phone" autocomplete="tel" inputmode="tel" required /></label>
        </div>
        <div class="quote__row">
          <label class="fld"><span>Town or ZIP</span><input type="text" name="town" autocomplete="postal-code" required /></label>
          <label class="fld"><span>What&rsquo;s the space for?</span>
            <select name="use" required><option value="">Choose one</option><option>Lawn</option><option>Dogs</option><option>Putting green</option><option>Kids / play area</option><option>Commercial or sports field</option></select>
          </label>
        </div>
        <button class="btn btn--primary" type="submit">Request a quote</button>
        <p class="quote__note">Free estimate, no pressure. Or call {phone} and skip the form.</p>
      </form>
    </div>
  </section>


  <!-- 15 · CLOSE — no invented scarcity. The reference claims a booking window; we have none stated. -->
  <section class="sec sec--dark on-dark close close--big turf-marks turf-marks--dark turf-marks--drift">
    <div class="wrap" data-reveal>
      <h2>Check every line on this page.</h2>
      <p class="close__p">The heat numbers are cited. The rebate search is written up county by county. The thirteen years are the owner&rsquo;s and the company started in 2024 — we print both. Ask about any line here and you will be told where the evidence stops. Then we measure your yard, free, and put a number in writing.</p>
      {phoneHref && <a class="btn btn--primary btn--lg" href={phoneHref} data-cta="call-close">Call {phone}</a>}
    </div>
  </section>


</Base>
